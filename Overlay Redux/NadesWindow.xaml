<Window x:Class="Overlay_Redux.NadesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Overlay_Redux"
        mc:Ignorable="d"
        Title="Nades" Height="195" Width="515" ResizeMode="NoResize"
        Background="{Binding WindowBackground}">
    <Window.Resources>
        <Style TargetType="Image">
            <Setter Property="Height" Value="131"/>
            <Setter Property="Width" Value="131"/>
        </Style>
        <Style TargetType="Border">
            <Setter Property="BorderThickness" Value="1.25"/>
            <Setter Property="BorderBrush" Value="{Binding BorderBrush}"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Padding" Value="2"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{Binding TextForeground}"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border>
            <Grid>
                <TextBlock Text="{Binding Arcs}"/>
                <Image Source="/Images/Arc.png"/>
            </Grid>
        </Border>
        <Border Grid.Column="1">
            <Grid>
                <TextBlock Text="{Binding Frags}"/>
                <Image Source="/Images/Frag.png"/>
            </Grid>
        </Border>
        <Border Grid.Column="2">
            <Grid>
                <TextBlock Text="{Binding Thermites}"/>
                <Image Source="/Images/Thermite.png"/>
            </Grid>
        </Border>
    </Grid>
</Window>
